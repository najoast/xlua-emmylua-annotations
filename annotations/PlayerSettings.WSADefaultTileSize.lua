---@class PlayerSettings.WSADefaultTileSize @Options for the application's default tile size.
---@field NotSet fun() @Doesn't use a default application tile size.
---@field Wide fun() @Uses the wide-sized tile as the default application tile size.
---@field Medium fun() @Uses the medium-sized tile as the default application tile size.
